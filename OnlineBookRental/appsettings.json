{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        // IMPORTANT: Replace "YourDbServerName" with your actual SQL Server name or IP.
        // Replace "YourDbName" with your desired database name.
        // Ensure SQL Server LocalDB or a full SQL Server instance is running.
        // For LocalDB, you might use "(localdb)\\mssqllocaldb" as the server name.
        "DefaultConnection": "Server=SWAPNIL-DESKTOP\\SQLEXPRESS;Database=OnlineBookRentalDb;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=True;"
    }
}